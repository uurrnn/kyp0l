.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Astro / Node
node_modules/
site/dist/
site/.astro/

# /data/ is now committed: the static site builds from it, and per the
# original plan it serves as the repo-backed database. Re-enable this if
# you want to develop without committing churn.
# data/

# Astro build artifacts and node_modules
site/dist/
site/node_modules/
site/.astro/
site/pnpm-lock.yaml
.astro/

# Probes/throwaway scripts kept around for reference but not shipped
# (we DO commit scratch/ in this branch — useful breadcrumbs for next session).
# scratch/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
